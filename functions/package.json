{
    "name": "sciscribe-functions",
    "description": "Cloud Functions for SciScribe Solutions — form handling and data storage",
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "serve": "npm run build && firebase emulators:start --only functions",
        "deploy": "npm run build && firebase deploy --only functions"
    },
    "engines": {
        "node": "18"
    },
    "dependencies": {
        "firebase-admin": "^12.0.0",
        "firebase-functions": "^5.0.0",
        "cors": "^2.8.5"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "typescript": "^5.3.0"
    },
    "private": true
}